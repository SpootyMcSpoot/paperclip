name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  ci:
    uses: Anomalous-Ventures/devops/.github/workflows/ci-nodejs.yml@main
    with:
      node-version: '22'
      package-manager: 'pnpm'
      working-directory: '.'
      run-tests: true
      run-docker-build: true
      # Override docker-registry in your fork's workflow file or use ghcr.io
      # docker-registry: 'your-registry.example.com'
    secrets: inherit

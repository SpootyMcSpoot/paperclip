# releases

Per-version release notes published alongside GitHub releases. One markdown file per version (e.g. `v0.3.1.md`).

The release pipeline in `scripts/release.sh` and `scripts/create-github-release.sh` consumes these files.

# @stapleai/shared

Cross-cutting types, zod schemas, and pure utilities shared between the server, UI, CLI, and adapters. No runtime dependencies on Express, React, or Node-only APIs where avoidable.

## Develop

```bash
pnpm --filter @stapleai/shared build
pnpm --filter @stapleai/shared typecheck
```
